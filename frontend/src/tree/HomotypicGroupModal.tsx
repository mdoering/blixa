// Placeholder — replaced in Task 5 with the detect/confirm modal (group synonyms into
// homotypic clusters + basionym relations). Kept minimal here so Task 4's Synonymy view (which
// renders this behind the "Group synonyms" button) is independently testable/green.
export default function HomotypicGroupModal(_: { pid: number; usageId: number; onClose: () => void }) {
  return null;
}
